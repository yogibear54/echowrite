# keyboard is vendored as a patched fork — installed separately by install.sh
# from ./vendor/keyboard (see vendor/keyboard/README.md for why).
sounddevice==0.4.7
scipy==1.11.4
replicate==0.34.0
pyperclip==1.8.2
pyautogui==0.9.54
python-dotenv==1.0.0
requests>=2.31.0
faster-whisper>=1.0.0  # Local Whisper provider (in-process speech-to-text)
pystray>=0.19.0  # GNOME top-bar (AppIndicator) status plugin

# Testing dependencies
pytest>=7.4.0
pytest-mock>=3.11.0
pytest-cov>=4.1.0
pytest-timeout>=2.1.0
responses>=0.23.0
